const song = require('./song.controller');
const set = require('./set.controller');
const author = require('./author.controller');
const genre = require('./genre.controller');
const place = require('./place.controller');

module.exports = {
  song, set, author, genre, place,
};
